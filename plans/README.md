# Implementation Plans

Generated by the improve skill on 2026-09-18 from a standard audit (recon +
4 parallel category audits, findings vetted against the code). Execute in the
order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

Repo notes every executor needs: no git repo exists (no SHA drift checks —
each plan carries content excerpts instead; do not init git). Canonical package
manager is npm after plan 004 (until then, use `npm` — all prior verification
ran under npm). If `node`/`npm` are not on PATH, prefix commands with
`export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"`. E2E scripts
(`scripts/*.mjs`) require a built `dist/` AND a real Netflix export folder;
without one, unit/build/lint gates are the bar and E2E steps are reported
skipped, never faked.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Honor the `topN` limit and establish the unit-test harness | P1 | S | — | DONE |
| 002 | Report true watch-hours on the Overview tab | P1 | S | 001 | DONE |
| 003 | Rank Devices regions once from combined counts | P2 | S | 001 | DONE |
| 004 | Standardize on npm as the single package manager | P1 | S | — | DONE |
| 005 | Self-host fonts and make the privacy claim true | P1 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 and 003 reuse the Vitest harness created in 001 — run 001 first.
- 004 and 005 both run `npm install`; do not execute them concurrently.
- Otherwise the five plans are independent and touch disjoint files.

## Findings considered and deferred (not planned now)

- CSP/hardening headers — needs a real hosting layer; the app is served ad-hoc today.
- Main-thread full-file CSV parse + retained blobs — valid, but M effort needing large-file regression coverage; revisit after unit baseline exists.
- Tab-scaffold consolidation (pending-guard/KPI rhythm ×10) — M effort, MED review risk; worth doing once churn resumes.
- Virtualizer dynamic measurement — MED fix-risk; current estimate holds for the common tables.
- Utils module split, `shadcn` dev-dep recategorization, AGENTS.md, portable Playwright scripts, per-tab contract checks, mobile 390px overlap, CSV export, shareable filter URLs — all valid backlog, none blocking the top 5.
